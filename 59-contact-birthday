public Boolean isBirthday(Contact c) {
    //code here

    if(month(c.BirthDate)==month(system.today()))
        {
            if(date(c.BirthDate)==date(system.today()))

        return true;

    }
    else
    {
        return false;
    }
}