public Boolean isBirthday(Contact c) {
    //code here


if( (c.BirthDate).Month() == system.today().Month())
{
    if( (c.BirthDate).Day() == system.today().Day()))
   return true;
}
   
else
{
    return false;
}
}