public void insertAtStart(String cityName, List<String> cities) {
    //code here

    for(integer i=1;i<=cities.size();i++)
    {
        cities[i].add(cities[i-1]);
    }
    cities[0].add(cityName);
    return cities;
}